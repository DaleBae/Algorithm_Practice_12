#include<iostream>

using namespace std;

int main(void) {
	int k, n, w;
	cin >> k >> n >> w;

	int price = 0;
	for (int i = 1; i <= w; i++) {
		price += i*k;
	}
	if (price >= n) {
		cout << price - n;
	}
	else {
		cout << 0;
	}
}
