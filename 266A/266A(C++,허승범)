#include <iostream>
using namespace std;

int main(void)
{
	short n, m = 0;
	char *str;
	cin >> n;
	str = new char[n];
	cin >> str;
	for (int i = 0; i < n - 1; i++)
	{
		if (str[i] == str[i + 1])
			m++;
	}
	cout << m;
}
