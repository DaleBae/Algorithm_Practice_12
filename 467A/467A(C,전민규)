#include <stdio.h>
int main(void){ int sum=0,num=0; int n=0,i=0; int p[100]={0}; int q[100]={0};
 scanf("%d",&n);
 for(i=0;i<n;i++){  scanf("%d %d",&p[i],&q[i]); }
 for(i=0;i<n;i++){  if(q[i]-p[i]>=2)   sum+=1; }
 printf("%d",sum);}
